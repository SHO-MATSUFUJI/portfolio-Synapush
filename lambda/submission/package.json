{
  "name": "submission",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.mjs --bundle --platform=node --target=node22 --format=esm --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" --outfile=dist/index.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.899.0",
    "@octokit/auth-app": "^7.2.1",
    "@octokit/rest": "^21.1.1",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
